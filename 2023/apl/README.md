```
Dyalog APL Competition 2023.

Workflow:
Phases are in .dws files. Can be opened in Dyalog using `)LOAD`.
`]defs' will show you all the definitions used.
`]export # /target/folder -arrays' will save all definitions including arrays as separate files.

Generated files from the workspaces are also provided.
```
